plugins {
    id 'java-library'
}

tasks.register('publish') {
    dependsOn subprojects*.publish
}

tasks.register('publishMavenPublicationToMavenLocal') {
    dependsOn subprojects*.publishMavenPublicationToMavenLocal
}